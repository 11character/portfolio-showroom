export default class EditorInterface {
    /**
     * 전역 조명 밝기 설정.
     * @param {float} intensity 밝기.
     */
    setLightIntensity(intensity) {
        console.error('need an interface.');
    }

    /**
     * 전역 조명 위치 설정.
     * @param {float} rad Y축 라디안 각도.
     */
    setLightHorizontalAngle(rad) {
        console.error('need an interface.');
    }

    /**
     * 전역 조명 밝기.
     */
    getLightIntensity() {
        console.error('need an interface.');
    }

    /**
     * 전역 조명 위치 각도.
     */
    getLightHorizontalAngle() {
        console.error('need an interface.');
    }
}